import colors from './colors';
export { colors };

export default {
  theme: {
    light: {
      bg: colors.white,
      color: colors.black
    },
    dark: {
      bg: colors.darkThemeGray,
      color: colors.white
    }
  }
};
